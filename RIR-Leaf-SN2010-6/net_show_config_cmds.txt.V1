net del all
net add time ntp server 0.cumulusnetworks.pool.ntp.org iburst
net add time ntp server 1.cumulusnetworks.pool.ntp.org iburst
net add time ntp server 2.cumulusnetworks.pool.ntp.org iburst
net add time ntp server 3.cumulusnetworks.pool.ntp.org iburst
net add time ntp source eth0
net add snmp-server listening-address localhost
net add routing defaults datacenter
net add routing log syslog informational
net add time zone Asia/Taipei
net add dhcp relay server 172.25.250.10
net add dhcp relay interface u,vlan1,240
net add interface swp1-22 breakout 1x
net add bond bond_up bond slaves swp22,swp21
net add bridge br_default ports glob,swp1-18,glob,swp19-20,bond_up
net add interface eth0 vrf mgmt
net add interface swp1-18 link speed 10000
net add interface swp1-18,21-22 link autoneg off
net add interface swp1-20 bridge access 1
net add interface swp1-20 stp bpduguard
net add interface swp1-20 stp portadminedge
net add interface swp1-20 stp portbpdufilter
net add interface swp19-20 link autoneg on
net add interface swp21-22 link fec rs
net add interface swp21-22 link speed 100000
net add vlan 1 post-up 'sysctl -w net.ipv6.conf.vlan1.disable_ipv6=1'
net add vlan 1 vlan-raw-device br_default
net add vlan 240 post-up 'sysctl -w net.ipv6.conf.vlan240.disable_ipv6=1'
net add vlan 240 vlan-raw-device bond_up
net add vrf mgmt ip address 127.0.0.1/8
net add vrf mgmt ipv6 address ::1/128
net add vrf mgmt vrf-table auto
net add hostname #cumulus
net commit

# There are some configuration commands that are not yet supported by nclu.
# The following will append those commands to the appropriate files.
# ========================================================================
net add bridge br_default pvid 1
net add bridge br_default vids 1
net add bridge br_default vlan-aware 
